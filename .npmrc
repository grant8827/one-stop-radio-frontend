# npm configuration for Railway deployment
# Avoid cache conflicts with Docker mount points

# Use a different cache location to avoid conflicts with Docker cache mounts
cache=/tmp/npm-cache

# Optimize for CI environment
prefer-offline=true
audit=false
fund=false
progress=false

# Reduce verbosity
loglevel=warn